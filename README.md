# Proiect C#/SQL
DealershipProiect is a C# Windows Forms application that manages data through a simple graphical interface connected to a SQL database. Users can interact with forms and controls to add, edit, or view information, while ADO.NET handles communication with the database. The project shows how a desktop interface can efficiently work with a structured data system.
